all:
  vars:
    ansible_user: "{{ lookup('env', 'K3S_SSH_USER') | default('pi', true) }}"
    ansible_python_interpreter: /usr/bin/python3
  children:
    control_plane:
      hosts:
        pi-cluster-1.local:
        pi-cluster-2.local:
        pi-cluster-3.local:
    workers:
      hosts:
        pi-cluster-4.local:
    k3s_cluster:
      children:
        control_plane:
        workers:
